[package]
name = "app"
version = "0.1.0"
edition = "2024"

[dependencies]
ssid = "0.1.2"
reqwest = { version = "0.12", features = ["json"] }
serde = { version = "1.0", features = ["derive"] }
tokio = { version = "1.0", features = ["full"] } # Für die asynchrone main
serde_json = "1.0"